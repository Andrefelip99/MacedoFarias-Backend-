spring.application.name=confeitariaMacedoFarias

spring.docker.compose.enabled=false

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

logging.level.org.springframework.security=TRACE

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


jwt.secret=${JWT_SECRET}

admin.email=${ADMIN_EMAIL:admin@confeitaria.local}
admin.password=${ADMIN_PASSWORD:changeMe123}

jwt.secret=${JWT_SECRET:MinhaChaveJWTUltraMegaSeguraComMaisDe32Caracteres123456}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

delivery.fee=${DELIVERY_FEE:6.0}
